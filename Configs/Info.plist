<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Partial Info.plist: GENERATE_INFOPLIST_FILE=YES merges the generated keys
	     (CFBundle*, scene manifest, permission strings, etc.) on top of this file.
	     Launch screen uses a storyboard (reliable full-screen art) rather than the
	     UILaunchScreen image dict, which doesn't render full-bleed images dependably. -->
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>NSFaceIDUsageDescription</key>
	<string>「简记」使用 Face ID 保护你的账单隐私，仅本人可查看。</string>
</dict>
</plist>
